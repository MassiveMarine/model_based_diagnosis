ab: "AB"
nab: "NAB"
neg_prefix: "not_"

props:
  - running(p2os)
  - running(sicklms)
  - running(tf_node)
  - running(map_server)
  - running(amcl)
  - running(move_base)
  - running(navigation)

rules:
  - NAB(p2os) -> running(p2os)
  - NAB(sicklms) -> running(sicklms)
  - NAB(tf_node) -> running(tf_node)
  - NAB(map_server) -> running(map_server)
  - NAB(amcl) -> running(amcl)
  - NAB(move_base) -> running(move_base)
  - NAB(navigation) -> running(navigation)


